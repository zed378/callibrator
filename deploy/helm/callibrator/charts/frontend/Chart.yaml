apiVersion: v2
name: frontend
description: >-
  Callibrator web application — Next.js 16 compiled to a standalone binary.
  NEXT_PUBLIC_* values are inlined at build time, so a different API URL or a
  tenant-pinned build requires a different image; this chart takes a tag and
  cannot configure them at runtime.
type: application
version: 0.1.0
appVersion: "0.1.0"
